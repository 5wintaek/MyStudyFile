/* const rawInput = prompt("원의 반지름을 입력하세요");

const diameter = Number(rawInput) * Number(rawInput) * 3.14;
const round = 2 * 3.14 * Number(rawInput);

alert(`원의 반지름:${rawInput} 원의 넓이:${diameter} 원의둘레:${round}`);

 */

const howMuch = prompt("그거얼마야?");

const a = Number(howMuch);
